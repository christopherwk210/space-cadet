{
  "name": "space-cadet",
  "version": "0.0.0",
  "main": "gulpfile.js",
  "description": "Framework for creating nostalgic web interfaces.",
  "scripts": {
    "build.prod": "gulp clean && gulp prod-sass && gulp minify-css && gulp css-lint",
    "build.dev": "gulp clean && gulp dev-sass && gulp css-lint && gulp dev-js"
  },
  "author": "Chris Anselmo",
  "license": "MIT",
  "devDependencies": {
    "gulp": "^3.9.1",
    "gulp-autoprefixer": "^3.1.0",
    "gulp-clean": "^0.3.2",
    "gulp-clean-css": "^2.0.7",
    "gulp-concat": "^2.6.0",
    "gulp-csslint": "^0.3.0",
    "gulp-sass": "^2.3.1",
    "merge-stream": "^1.0.0"
  }
}
